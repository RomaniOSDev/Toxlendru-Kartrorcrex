import SwiftUI

/// Нативная часть приложения (онбординг + основной таббар).
struct GameView: View {
    var body: some View {
        ContentView()
    }
}

#Preview {
    GameView()
        .preferredColorScheme(.dark)
}
